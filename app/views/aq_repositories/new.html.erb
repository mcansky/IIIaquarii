<% content_for :head do %>
  <title><%= t(:new, :scope => :repositories) %> - <%= Settings.application.name %></title>
<% end %>

<div class="grid_4 prefix_4 suffix_4">
  <div id="repository_form">
    <h2><%= t(:new, :scope => :repositories) %></h2>

    <%= render "form", :repository => @repository %>
  </div>
</div>
<div class="clear"></div>
